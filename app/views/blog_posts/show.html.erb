<h1><%= @blog_post.title %></h1>
<p><%= @blog_post.body %></p>
<% if user_signed_in? %>
    <%= link_to 'Edit', edit_blog_post_path %>
    <%= button_to 'Delete', blog_post_path(@blog_post), method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-danger' %>
<% end %>