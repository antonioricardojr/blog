<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Blog</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-100 text-gray-900 font-sans">
    <!-- Navbar -->
    <nav class="bg-white shadow-md">
      <div class="container mx-auto px-4 py-4 flex justify-between items-center">
        <a href="/" class="text-2xl font-bold text-blue-600">Blog</a>
        
        <div>
          <% if user_signed_in? %>
            <p>Hello, <%= current_user.email %></p>
            <%= button_to "Log out", destroy_user_session_path, method: :delete, class: "bg-red-500 text-white font-semibold py-2 px-4 rounded hover:bg-red-700 float-right" %>
          <% else %>
            <%= link_to "Log in", new_user_session_path, class: "text-blue-600 hover:text-blue-800 font-semibold" %>
          <% end %>
        </div>
      </div>
    </nav>

    <!-- Flash Messages -->
    <div class="container mx-auto px-4 py-4">
      <% if notice %>
        <div class="bg-green-100 text-green-800 border border-green-200 rounded-md p-4 mb-4">
          <%= notice %>
        </div>
      <% end %>
      <% if alert %>
        <div class="bg-red-100 text-red-800 border border-red-200 rounded-md p-4 mb-4">
          <%= alert %>
        </div>
      <% end %>
    </div>

    <!-- Main Content -->
    <div class="container mx-auto px-4 py-8">
      <%= yield %>
    </div>

    <!-- Footer -->
    <footer class="bg-white shadow-md mt-8">
      <div class="container mx-auto px-4 py-4 text-center text-gray-600">
        &copy; <%= Time.current.year %> Blog. All rights reserved.
      </div>
    </footer>
  </body>
</html>
